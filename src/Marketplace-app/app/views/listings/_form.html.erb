<%= bootstrap_form_with(model: listing, local: true) do |form| %>
  <% if listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
        <% listing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.select :brand_id, @brands.map {|s| [s.title, s.id ]} %>
  </div>

  <div class="field">
    <%= form.select :style_id, @styles.map {|s| [s.title, s.id ]} %>
  </div>

  <div class="field">
    <%= form.select :size_id, @sizes.map {|s| [s.title, s.id ]} %>
  </div>

  <div class="field">
    <%= form.select :state_id, @states.map {|s| [s.title, s.id ]} %>
  </div>

  <div class="field">
    <%= form.select :city_id, @cities.map {|s| [s.title, s.id ]} %>
  </div>

  <div class="field">
    <%= form.select :postcode_id, @postcodes.map {|s| [s.title, s.id ]} %>
  </div>

  <div>
    <%= form.file_field :picture, accept:
    'image/png,image/gif,image/jpeg,image/jpg' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
